package template

import (
	"testing"
)

func TestLocalization(t *testing.T) {

	/*t.Run("Load localization files", func(t *testing.T) {
		pkg, err := importer.Default().Import("")
		if err != nil {
			fmt.Printf("error: %s\n", err.Error())
			return
		}
		for _, declName := range pkg.Scope().Names() {
			fmt.Println(declName)
		}

	})*/
}
